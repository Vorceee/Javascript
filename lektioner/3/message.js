const message = () => {
    const name = "Felix";
    const age = 19;
    const loves = ["JavaScript", "Video Games"];
    return "This is a message from " + name + ". he is " + age + " and he loves " + loves[0] + " and " + loves[1];
}

export default message;